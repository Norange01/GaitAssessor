clear all
clc
 
arduino=serialport("/dev/cu.usbserial-1420",9600);
 
fopen(arduino);
 
x=linspace(1,100);
y = zeros(1,100);
   
for i=1:length(x)
	n(fscanf(arduino, '%d'));
end
	
fclose(arduino);
disp('making plot..')
plot(x,y);